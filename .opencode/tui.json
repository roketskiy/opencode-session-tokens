{
  "$schema": "https://opencode.ai/config.json",
  // 生产用法：安装 npm 包后启用
  // "plugin": [
  //   ["opencode-session-tokens", { "format": "details" }]
  // ]
  //
  // 本地开发用法：直接加载仓库源码（相对路径相对于本配置所在目录）
  "plugin": ["../src/tui.tsx"]
}